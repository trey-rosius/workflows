This folder contains an updated version of the platform https://educloud.academy. All workshops have been broken down into vector embeddigns and saved into s3vector store. Theres a chatbot to answer user query based on courses. There's also a chat interface to help users build curriculum or learning paths by assessing them. But the app still has lots of bad code, no tests and things arent' working. for instance, when a user asks for their curriculum to be built, i want the app to get results from tavily search and combine with queires from the s3 vector database and return the user a list of courses on the platfrom, plus courses that arent available on the platform, but can be created by the platfrom admin. It shouldn't suggest courses from other platforms. I also want all user prompts to be saved for auditing later. 